//in ts we can use a shortcut to define functions
            //parameter of message
let doLog = (message) => {
    //code block
    console.log(message);
}
//if it's a short function you can make it one line
let log = (message) => console.log(message);
//no params
let log2 = () => console.log('hi');